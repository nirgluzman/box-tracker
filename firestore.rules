rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /boxes/{box} {
      allow read, write: if request.auth != null;
    }
    match /rooms/{room} {
      allow read, write: if request.auth != null;
    }
  }
}
