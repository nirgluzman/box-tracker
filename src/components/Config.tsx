// SPEC 6.5 — Config. Skeleton; full flow built in Phase 5.
// TODO: room manager (add/edit/delete, color picker), range-start auto-suggest
// + overlap warnings, CSV download/upload, orphaned-photos cleanup.
export default function Config() {
  return (
    <section className="screen">
      <h2>Config</h2>
      <p>Room manager — not yet implemented.</p>
    </section>
  )
}
